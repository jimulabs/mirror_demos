<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

    <ListView
            android:id="@+id/mail_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:dividerHeight="1dp"
            android:orientation="vertical"
            />

    <ImageView
            android:id="@+id/pencil"
            android:layout_width="@dimen/pencil_width"
            android:layout_height="@dimen/pencil_width"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:layout_marginRight="@dimen/pencil_margin_right"
            android:layout_marginBottom="@dimen/pencil_margin_bottom"
            android:src="@drawable/pencil"/>
</RelativeLayout>

